git and git hub

git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/arpit-code13/FSD_backend_3rds_sem.git
git push -u origin main

git status
git log 


//to create react file

npm create vite@latest 

//
rafc to create direct jsx funtion 
//////////////////////////////////////////////////////////////////////
Every time you add/change code

git add .
git commit -m "add login API"
git push

if you want to add only a specific section

Backend only:

git add Classroom/backend
git commit -m "update backend"
git push

f you want to add only a specific section

Backend only:

git add Classroom/backend
git commit -m "update backend"
git push

One specific file:

git add Classroom/backend/server.js
git commit -m "update server"
git push
⭐ Recommended nor


Recommended normal command

Just use:

git add .
git commit -m "update code"
git push


////////////////////////////////
the way to call in chorome 
http://localhost:3000/sys